#!/usr/bin/env python3
"""Module that provides a function for finding the floor of a float."""


def floor(n: float) -> int:
    """Return the floor of a float.

    Args:
        n: float to be floored

    Returns:
        The floor of the float as an integer
    """
    return int(n)